{
  "name": "mnemon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile bin/mnemon",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "1.3.12"
  }
}
